This branch manages Travis CI integration to build the Linux Kernel.
While the builds are functioning correctly, they are failing due to 
build timeout limits [1]

[1] https://docs.travis-ci.com/user/customizing-the-build/#build-timeouts
